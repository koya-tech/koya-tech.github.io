
/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // i18n: {
    //     locales: ['en', 'jp'],
    //     defaultLocale: 'en',
    // },
}

module.exports = nextConfig;
